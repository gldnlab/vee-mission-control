# Convex — get this from convex.dev after running `npx convex dev`
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# API secret — Vee uses this to authenticate when writing to Mission Control
# Generate: openssl rand -hex 32
API_SECRET=your-secret-here
